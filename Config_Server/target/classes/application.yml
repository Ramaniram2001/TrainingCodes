server:
  port: 8888
  
  
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Ramaniram2001/Cloud_Config.git
          search-paths:
          - '{application}'
          
management:
  endpoints:
    web:
      exposure:
        include:
        - "*" 
      